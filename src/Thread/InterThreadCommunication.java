package Thread;

public class InterThreadCommunication {

	/**
	 * Java provides methods like wait(),notify(),notifyAll() to faciliatate communication b/w threads.
	 * 
	 * -> wait ()-causes current thread to wait until another thread invokes notify()
	 * -> notify() - waiting for single thread that is waiting for object monitor
	 * -> notifyAll()- waiting for all thread that is waiting for object monitor
	 */
}
