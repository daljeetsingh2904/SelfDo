package Thread;

/**
 * 
 * ->Thread.sleep() causes the current thread to stop execution for specific period, this is efficent means of making processor time 
 * available to other thread of application
 * 
 * ->There are two overloaded methods of sleep in one you can specify time in millisecond and other in nanosecond
 * ->Sleep method can throw interrupted exception , this is an excpetion that sleep throws when another thread interrupts the current thread
 * while sleep is active.  
 * 
 *
 */
public class SleepMethodInThreadClass {

}
