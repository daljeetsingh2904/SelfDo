package Thread;

public class ThreadTheory {

	/**
	 * -> A thread is lightweight subprocess,the smallest unit of processing.
	 *    It is seprate path of execution in program.
	 *    
	 *  Why use threads?
	 * 
	 *  -> Allows multiple tasks to run simultaneously,improving perfomance.
	 *  -> Take advantage of multi-core processor by running task in parallel.
	 *  -> Keeps application responsive by performing background tasks without freezing application   
	 *
	 *  Two ways to create a thread:
	 *  ->Using extend keyword.
	 *  ->Using runnable interface.
	 */
}
